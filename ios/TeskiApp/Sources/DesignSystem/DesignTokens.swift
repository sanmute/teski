import SwiftUI

enum DesignTokens {
    static let cornerRadius: CGFloat = 12
    static let padding: CGFloat = 16
    static let agendaGradient = LinearGradient(colors: [.teal, .mint], startPoint: .leading, endPoint: .trailing)
}
